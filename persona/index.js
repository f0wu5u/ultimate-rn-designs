/**
 * @format
 */

import {AppRegistry} from 'react-native';
import AppNavigation from './src/container';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => AppNavigation);
